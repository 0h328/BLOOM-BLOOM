import styled from 'styled-components';

const Layout = styled.div`

  .choose_wrapper {
    text-align: center;
    margin-top: 30px;
  }
  
  .wrapper_image, .ribbon_image, .stalk_image {
    margin: 7.3px 7.3px;
  }

  .select_items {
    display: flex;
    flex-wrap: wrap;
    margin: 30px; 0;
  }
  
  .k {
    display: flex;
    flex-wrap: wrap;
    flex-direction: column;
  }

`;

export default Layout